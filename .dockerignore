# Keep secrets, installed deps, and build output out of the image/context.
**/node_modules
**/.env
**/.env.*
**/dist
**/*.tsbuildinfo
.git
.claude
# The agent image only needs shared/ + ai-agent/ (see ai-agent/Dockerfile).
frontend
mcp-server
supabase
